class F3{
    constructor(opts){
        this.x2State = {uuid:''};
        this.username = opts.username.trim();
        this.password = opts.password.trim();
        this.host = opts.host.trim();
    }
}
